package http

// SetAuth tries to find the auth config for the cluster ans sets it on the http request.
/*func SetAuth(request *http.Request, clusterURL string) {
	username, password, err := config.GetAuthConfig(clusterURL)
	if err != nil {
		// No auth config found.
		return
	}

	request.SetBasicAuth(username, password)
}*/
